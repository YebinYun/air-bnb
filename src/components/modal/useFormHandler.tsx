import { TextField } from "@mui/material";
import React from "react";

const useFormHandler = ({ width, padding, margin, textAlign }: any) => {
  return (
    <>
      <div>useFormHandler</div>
      <input style={{}} />
    </>
  );
};

export default useFormHandler;
